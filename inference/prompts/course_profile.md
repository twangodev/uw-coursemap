# Course profile

Create a concise profile from the supplied catalog data. Describe skills only when
the description supports them. Each topic and skill needs a short verbatim evidence
quote. Use empty arrays when evidence is insufficient. Do not invent assessments,
workload, instructor quality, difficulty, career outcomes, or prerequisites.
Return only the requested JSON object.
