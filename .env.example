# Frontend endpoints; the replacement Worker is not implemented yet.
PUBLIC_API_URL='https://static.uwcourses.com'
PUBLIC_SEARCH_API_URL='https://search.uwcourses.com'
CROWDIN_API_TOKEN='CHANGEME'

# Local data pipeline (inference runs in its separate environment).
COURSEMAP_WORKSPACE='./generation/.cache/pipeline'
MADGRADES_API_KEY='CHANGEME'
